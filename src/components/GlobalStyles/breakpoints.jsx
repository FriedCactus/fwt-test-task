const sizes = {
  l: "1366px",
  m: "1024px",
  s: "768px",
  xs: "320px",
};

const breakpoints = {
  l: `min-width: ${sizes.l}`,
  m: `min-width: ${sizes.m}`,
  s: `min-width: ${sizes.s}`,
  xs: `min-width: ${sizes.xs}`,
};

export default breakpoints;
