const sizes = {
  l: "1366px",
  m: "1024px",
  s: "768px",
};

const breakpoints = {
  l: `min-width: ${sizes.l}`,
  m: `min-width: ${sizes.m}`,
  s: `min-width: ${sizes.s}`,
};

export default breakpoints;
